FROM jenkins/jenkins:lts

USER root

# Install Docker CLI, Python, Node.js, and other required tools
RUN apt-get update && apt-get install -y \
    docker.io \
    python3 \
    python3-pip \
    nodejs \
    npm \
    git \
    curl \
    && rm -rf /var/lib/apt/lists/*

USER jenkins
